{
  "name": "fitzone",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "client:dev": "npm run dev --workspace frontend",
    "server:dev": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "api:smoke": "npm run smoke --workspace backend",
    "seed:membership-plans": "npm run seed:membership-plans --workspace backend",
    "seed:trainers": "npm run seed:trainers --workspace backend",
    "seed:class-schedule": "npm run seed:class-schedule --workspace backend",
    "seed:site-settings": "npm run seed:site-settings --workspace backend",
    "seed:all": "npm run seed:all --workspace backend",
    "format": "prettier --write .",
    "preview": "npm run preview --workspace frontend"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
